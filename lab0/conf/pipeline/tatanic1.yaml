working_dir: ${hydra:runtime.cwd}

paths:
  data_parent_dir: ${working_dir}/data
  models_parent_dir: ${working_dir}/models
  reports_parent_dir: ${working_dir}/reports
  figures_parent_dir: ${working_dir}/figures
  data:
    raw_data: ${paths.data_parent_dir}/raw
    interim_data: ${paths.data_parent_dir}/interim
    processed_data: ${paths.data_parent_dir}/processed
    external_data: ${paths.data_parent_dir}/external

dataset:
  target_col: "Survived"
  id_col: "PassengerId"
